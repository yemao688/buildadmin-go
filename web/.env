# 本文件只放所有机器共享的默认值。
# 机器/项目级覆盖（如多项目并行开发要改端口）：新建 .env.local（或 .env.<mode>.local）覆盖同名键，
# 这类文件已被 gitignore（*.local），不会产生 git 变动。
# 优先级：.env < .env.local < .env.[mode] < .env.[mode].local

# port 端口号
VITE_PORT = 9918

# open 运行 npm run dev 时自动打开浏览器
VITE_OPEN = false
